-- TABLA "leche"
CREATE TABLE leche (
id_leche INT PRIMARY KEY AUTO_INCREMENT NOT NULL,
vaca_numero INT,
parto_numero INT,
nombre_operador VARCHAR(30),
cria_sexo VARCHAR(50),

cria_fecha_venta VARCHAR(20),
leche_observaciones VARCHAR(200),
fecha_registro DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);